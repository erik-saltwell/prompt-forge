name: valid_json
description: "False / atomic / deterministic — syntactic format check."
axes: { requires_gt: false, structure: atomic, judgment: deterministic }
criterion: "Output is a syntactically valid JSON object."
requires_ground_truth_expected: false
